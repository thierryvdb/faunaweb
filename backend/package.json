{
  "name": "fauna-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "predev": "node -e \"const v=process.versions.node.split('.')[0];if(v<20){console.error('\\n❌ Node.js 20+ required. Current:',process.version,'\\nPlease upgrade: https://nodejs.org/\\n');process.exit(1)}\"",
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "tsc --noEmit",
    "check-node": "node -e \"console.log('Node.js version:',process.version);const v=process.versions.node.split('.')[0];if(v>=20){console.log('✅ Version OK')}else{console.error('❌ Requires Node.js 20+');process.exit(1)}\"",
    "check": "node check-setup.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^10.0.0",
    "@fastify/multipart": "^9.0.1",
    "chart.js": "^4.4.3",
    "chartjs-node-canvas": "^5.0.0",
    "docx": "^8.5.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.2.0",
    "fastify-plugin": "^5.0.1",
    "pdfkit": "^0.15.0",
    "pg": "^8.11.3",
    "sharp": "^0.33.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/pdfkit": "^0.13.6",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.17.0",
    "typescript": "^5.4.5"
  }
}
